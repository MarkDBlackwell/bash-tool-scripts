#!/bin/sh
# Author: Mark D. Blackwell
# Change dates:
# (mdb) 2013-03-09 - created

# Find not.
# Usage:
#   find-not

# Implicitly, the regular expression includes both filename ends (without ^$).

# Example (to find un-minified (not -.min.css) CSS files:
# find * -regex '.+[^n]\.css'
